# Required application credentials. Never commit your real token.
DISCORD_TOKEN=
APPLICATION_ID=

# Optional: when set, commands are synced only to this development server.
# Leave unset for production/global command registration.
DEV_GUILD_ID=

# Optional: relative paths are resolved from the project directory.
DATABASE_PATH=data/shoe_bot.sqlite3

